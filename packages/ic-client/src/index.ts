// Infinite Campus auth handshake + fetch client + response parsers, ported
// in from mobile/lib/icClient.ts, icApi.ts, icParsers.ts (the more mature
// version) in a later pass. Kept fetch-based and environment-agnostic so
// both apps/web's API routes and apps/mobile import the same code.
export {};
